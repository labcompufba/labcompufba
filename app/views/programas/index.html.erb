     <p id="notice"><%= notice %></p>
      <div id="list" class="row">
        <div class="table-responsive col-md-12">
      <table class="table table-striped" cellspacing="0" cellpadding="0" width="100%" height="100" >
        <thead>
          <tr>
            <th>Nome</th>
            <th>Sigla</th>
            <th>Local</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          

  <tbody>
    <% @programas.each do |programa| %>
      <tr>
        <td><%= programa.nome %></td>
        <td><%= programa.sigla %></td>
        <td><%= programa.local %></td>
        <td class="actions">
               <%= link_to 'Visualizar', programa, class: 'btn btn-success btn-xs' %>
               <% if can? :pesq, Instituto %>
                <%= link_to 'Editar', edit_programa_path(programa) , class: 'btn btn-warning btn-xs' %>
                <%= link_to 'Deletar', programa, method: :delete, data:{ confirm: 'você tem certeza?' }, class: 'btn btn-danger btn-xs' %>
               <%end%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<a class="btn btn-primary btn-sm" <%= link_to 'Novo Programa', new_programa_path %></a>
